name: slack_github_action

on:
  push:
    branches:
      - feature

jobs:
  slack_notifier:
    # runs-on: manish_org_runner
    runs-on: manish_linux
  

    steps:
      - name: Set up JDK 17
        uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: '17'
 
      # - name: Set PowerShell Execution Policy
      #   run: |
      #      Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser -Force
      - name: Check out repository
        uses: actions/checkout@v2

# JFROG PART

      - name: Install JFrog CLI
        run: |
          curl -fL https://getcli.jfrog.io | sh
          sudo mv jfrog /usr/local/bin/ 
        # Assuming jfrog is in the current directory


 
      - name: Build Maven project 
        run: mvn  clean install
             # $env:MAVEN_EXIT_CODE=$LASTEXITCODE
          
      - name: Generate code coverage report
        run: |
          
          mvn jacoco:report
      - name: Publish code coverage result
        uses: actions/upload-artifact@v2
        with:
          name: code-coverage
          path: target/site/jacoco/index.html

      - name: Convert to non-shallow clone
        run: git fetch --unshallow

      # - name: SonarQube Scan
      #   # run: mvn sonar:sonar -Dsonar.host.url="${SONARQUBE_URL}" -Dsonar.login="${SONAR_QUBE_GH_10}" -Dsonar.sources=. -Dsonar.java.binaries=./target
      #   run: mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar
      #   env:
      #     SONAR_TOKEN: ${{ secrets.SONAR_QUBE_GH_10 }}
      #     SONAR_HOST_URL: ${{ secrets.SONARQUBE_URL }}

      
     


      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan-action@v2
        with:
          projectBaseDir: .
          args: >
            -Dsonar.organization=manish-alm-demo 
            -Dsonar.projectKey=manish-alm-demo_gh-action-10
            -Dsonar.sources=.
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.java.binaries=./target/classes
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_CLOUD_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_URL }}


      - name: Set JFrog CLI Path
        run: |
         export PATH=$PATH:/usr/local/bin
 
      - name: Push Artifacts to JFrog
        run: |
          jfrog rt u target/*.jar gitaction-libs-snapshot-local/ --url=https://almdemogh.jfrog.io/artifactory/gitaction-libs-snapshot-local/ --access-token=eyJ2ZXIiOiIyIiwidHlwIjoiSldUIiwiYWxnIjoiUlMyNTYiLCJraWQiOiJlcWpGcVNxTmRBTDExRVRTYmUzSWQxM2JyTDJLekxoRXBYY19sOXBjVWkwIn0..a6I0tBvDHzfpOnORnLSMqAI7QwpU18kQkR8D4BBBoXvgMlKDc-EIVgwJD2TNYnnPHse4sxQ1EZjCpebS45vymn1i_isfrR3NMUQaVWk08SlNvy6bS8tXGLTDglOfGNszmoyRZDfA62mXGULXGEp_n-C0wKCevkQS_oVimTb-FGZjHzUh5Ll7qgsahKMSMQHDpQ2eypsFVTGhYi_JU0A4_NuS9997xyqBRWIwvU60K3UQNbevKnR1fzZwgximp0ghGZI7YuWq3tojniMHFQCM-ZYF3cCUAwrL3Waj9Fi4bT5JvudpHRXtde-50ZN2S132SfJy6zjs2L-AmevvqIi04w
 
 




      - name: Send notification on Slack using Webhooks - Success
        if: ${{ success() }}
        uses: slackapi/slack-github-action@v1.24.0
        with:
          payload: |
            {
              "text": "Maven project built successfully -- Send build sttaus to slack."
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_API_URL }}

      - name: Send notification on Slack using Webhooks - Failure
        if: ${{ failure() }}
        uses: slackapi/slack-github-action@v1.24.0
        with:
          payload: |
            {
              "text": "Maven build failed. Please check the build logs for details and resolve it."
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_API_URL }}

      # - name: SonarQube Scan
      #   uses: kitabisa/sonarqube-action@v1.2.0
      #   with:
      #     host: ${{ secrets.SONARQUBE_URL }}
      #     login: ${{ secrets.SONAR_QUBE_GH_10 }}


      
